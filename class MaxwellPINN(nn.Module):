class MaxwellPINN(nn.Module):
    """
    Field representation:
    f(x,y,z,t) = [Ex, Ey, Ez, Bx, By, Bz]

    Input:  (x, y, z, t)
    Output: (E_x, E_y, E_z, B_x, B_y, B_z)

    Neural network approximates the electromagnetic
    field over spacetime.
    """
